describe(
		'OpenLayers.Control.KeyboardClick', function() {
			it(
					'Object type bestaat', function() {
						expect(
								OpenLayers.Control.KeyboardClick).toBeDefined();
					});
		});
