/**
 * Jasmine Unit tests voor OpenLayers.Handler.KeyboardPoint.
 * 
 * @author prinsmc
 */
describe('OpenLayers.Handler.KeyboardPoint', function() {
	it('Object type bestaat', function() {
		expect(OpenLayers.Handler.KeyboardPoint).toBeDefined();
	});
});
